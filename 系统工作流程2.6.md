# 游戏包名爬虫系统 v2.6 — 工作流程文档

## 一、系统架构

```
┌─────────────────────────────────────────────────┐
│  浏览器 (Vue3 + Element Plus + CSS 主题系统)      │
│  http://127.0.0.1:8000                            │
│                                                    │
│  styles/global.css    全局主题 (350 行 CSS 变量)   │
│                                                    │
│  App.vue (主布局 - 4个标签页)                      │
│  ├── PackageInput.vue   包名输入 + emoji 操作      │
│  ├── ResultTable.vue    结果表格 + 应用信息卡片     │
│  ├── BatchPanel.vue     批量 Excel + 完成弹窗       │
│  ├── DownloadQueue.vue  下载队列 (卡片化)          │
│  └── SettingsPanel.vue  系统设置 + 主题切换         │
└──────────────┬──────────────────────────────────┘
               │ HTTP REST + WebSocket
               ▼
┌─────────────────────────────────────────────────┐
│  FastAPI 后端 (127.0.0.1:8000)  v2.6.0           │
│                                                    │
│  main.py             入口 + 生命周期               │
│  api/routes.py        28 个 REST/WS 端点           │
│  api/websocket.py     WebSocket 推送进度           │
│                                                    │
│  ┌─ Orchestrator (core/orchestrator.py) ──────┐  │
│  │  并发调度爬虫, 版本聚合对比, TTL 缓存         │  │
│  │  聚合 app_name + whats_new (v2.6)           │  │
│  │                                              │  │
│  │  FAST_SCRAPERS (秒级, curl_cffi Fetcher)    │  │
│  │    GooglePlayScraper  (lang=zh, 中文名+更新) │  │
│  │    ApkpureScraper     (搜索→详情 + 名/内容)   │  │
│  │    ApkcomboScraper    (API 302 + 名/内容)    │  │
│  │                                              │  │
│  │  SLOW_SCRAPERS (30-90s, StealthySession)    │  │
│  │    ApkmirrorScraper   (搜索→详情 多步跳转)    │  │
│  │    ApkvisionScraper   (StealthySession+CF)   │  │
│  └──────────────────────────────────────────────┘  │
│                                                    │
│  ┌─ Cache Layer (core/cache.py) ──────────────┐  │
│  │  ScraperCache    TTL 内存缓存 (300s 可配)    │  │
│  │  SlowTaskStore   慢速异步任务状态存储         │  │
│  └──────────────────────────────────────────────┘  │
│                                                    │
│  Download Manager       aiohttp + Playwright 降级   │
│  Browser Manager        Playwright 持久上下文 (后台) │
│  Batch Manager          批量任务 + Semaphore 并发    │
│  Memo Store             SQLite 版本记忆             │
│                                                    │
│  config.json            用户配置 (18 项)             │
│  data/crawler.db        SQLite 数据库               │
│  frontend/dist/         Vue3 构建产物 (FastAPI托管)  │
└─────────────────────────────────────────────────┘
```

### 三级 HTTP 后端对比

| 后端 | 底层 | Cloudflare | 单页耗时 | 适用站点 |
|------|------|-----------|---------|---------|
| **Fetcher** | `Scrapling Fetcher` (curl_cffi + browserforge) | ❌ 被拦截 | 1-5s | Google Play / APKPure / APKCombo |
| **JS Render** | `StealthySession` (Chromium, CF=false) | ❌ 不等待 | 5-15s | APKPure 搜索页 JS 降级 |
| **StealthySession** | `StealthySession` (Chromium, CF=true) | ✅ 自动解 JS Challenge | 30-90s | APKMirror / APKVision |

---

## 二、排查模式

| 模式 | 路由 | 站点 | 后端 | 耗时 | 缓存 |
|------|------|------|------|------|------|
| **快速排查** (默认) | `/api/fetch` | Google Play + APKPure + APKCombo | Fetcher | ~10s | fast |
| **快速排查** | `/api/fetch/fast` | 同上 | Fetcher | ~10s | fast |
| **慢速排查** | `/api/fetch/slow` | APKMirror + APKVision | StealthySession | 30-90s | slow |
| **慢速异步** | `/api/fetch/slow/async` | APKMirror + APKVision | StealthySession | 30-90s (后台) | slow |
| **全量排查** | `/api/fetch/all` | 全部 5 个 | Fetcher + Stealthy | 30-100s | all |
| **多包名排查** | `/api/fetch/batch` | 同上, Semaphore 限流 | 对应模式 | ~12s/3包名 | 继承模式缓存 |

---

## 三、单包名快速排查流程

```
[用户] 输入 com.tencent.ig
   │
   ▼
[前端] POST /api/fetch  {package, expected_version, expected_version_code, save_memo: true}
   │
   ▼
[Orchestrator] query_fast()
   │
   ├─ 检查 ScraperCache (TTL=300s) → 命中则直接返回
   ├─ Semaphore(scraper_concurrency=4) 限流
   ├─ asyncio.gather 并发 3 个快速爬虫:
   │
   ├─ GooglePlayScraper (lang=zh, country=cn)
   │   ├─ 注入代理到 urllib 环境变量
   │   ├─ google-play-scraper.app(package, lang="zh", country="cn")
   │   ├─ 提取: version, title→app_name, recentChanges→whats_new
   │   └─ version="4.4.0", app_name="PUBG MOBILE", whats_new="..."
   │
   ├─ ApkpureScraper
   │   ├─ Step 1: Fetcher 获取搜索页 apkpure.com/search?q={pkg}
   │   │   └─ 若被 CF 拦截 → 回退到 apkpure.net
   │   ├─ Step 2: 正则提取详情页 URL (含包名匹配, 排除 /search)
   │   │   └─ 若 Fetcher 无链接 → JS Render 降级取详情
   │   ├─ Step 3: Fetcher 获取详情页
   │   ├─ Step 4: extract_both(html, package) → version + version_code
   │   │   └─ 包名关联: data-dt-package_name + .{0,500}? 限定
   │   └─ Step 5: _extract_app_name + _extract_whats_new (v2.6)
   │       └─ data-dt-title / data-dt-whatsnew 属性提取
   │
   └─ ApkcomboScraper
       ├─ Fetcher 获取 apkcombo.com/api/app/{pkg} → 302 详情页
       ├─ extract_both(html, package) → version + version_code
       └─ _extract_app_name + _extract_whats_new (v2.6)
           └─ itemprop="name" / data-role="whatsnew" 属性提取
   │
   ▼
[结果聚合] _build_fetch_result()
   best_version("4.4.0") — 多源共识判定 (优先 Google Play)
   best_version_code("21118") — APKPure 包名验证获取
   best_app_name("PUBG MOBILE") — 优先级: GP > APKPure > APKCombo (v2.6)
   best_whats_new("What's New...") — 优先级同上 (v2.6)
   version_name_compare: "matched:4.3.0 → 4.4.0" (NEWER)
   version_code_compare: "matched:vc:21000 → vc:21118" (NEWER)
   │
   ▼
[写入缓存] ScraperCache.set(package, "fast", result) TTL=300s
   │
   ▼
[记忆化] save_memo=true → MemoStore.upsert()
   │
   ▼
[前端] ResultTable 展示 + 应用信息卡片 (v2.6: 中文名 + 更新内容)
```

---

## 四、慢速异步排查流程

```
[前端] POST /api/fetch/slow/async  {package, expected_version, expected_version_code}
   │
   ▼
[后端] SlowTaskStore.create() → task_id = "slow_xxxxxxxx"
   ← {"task_id": "slow_xxx", "status": "pending"}
   │
   ▼
[后台] asyncio.create_task(_run_slow())
   ├─ query_slow(package) → APKMirrorScraper + ApkvisionScraper
   │   ├─ 检查 ScraperCache("slow") → 命中则跳过
   │   └─ StealthySession 每站点 30-90s
   │
   ├─ 成功 → SlowTaskStore.complete(task_id, result)
   │   └─ WebSocket 推送: {type: "slow_task_completed"}
   │
   └─ 失败 → SlowTaskStore.fail(task_id, error)
       └─ WebSocket 推送: {type: "slow_task_error"}
   │
   ▼
[前端] 轮询 GET /api/fetch/slow/result/{task_id}
```

---

## 五、版本比对逻辑

```
两条线独立对比，互不覆盖:

版本号对比 (优先):
  compare_version_codes(expected_vc, best_vc)
    -1 → newer → "vc:21000 → vc:21118"
     0 → matched → "版本号一致 (vc:21118)"
     1 → older → "vc:21000 → vc:20000"
  None → 无法解析，回退到版本名对比

版本名对比 (fallback):
  compare_versions(best_v, expected_v)
    1 → newer → "4.3.0 → 4.4.0"
    0 → matched (含 normalize 后一致)
    -1 → older → "4.5.0 → 4.4.0"

综合状态 (compare_status):
  优先 version_code 对比结果，其次 version_name 对比结果
  均无 → NOT_FOUND
```

### best_version / best_version_code 判定

| 条件 | 策略 |
|------|------|
| 同一值 ≥2 源一致 | 直接采纳 |
| 仅 1 源有结果 | 采纳该源 |
| 多源有结果无共识 | 优先 Google Play，否则取数值最大 |

### app_name / whats_new 聚合 (v2.6 NEW)

| 优先级 | 来源 | 提取方法 |
|--------|------|---------|
| 1st | Google Play | `google-play-scraper` 直接返回 `title` + `recentChanges` |
| 2nd | APKPure | HTML 解析 `data-dt-title` + `data-dt-whatsnew` 属性 |
| 3rd | APKCombo | HTML 解析 `itemprop="name"` + `data-role="whatsnew"` |

---

## 六、版本号解析 (13 模式，按优先级)

| 优先级 | 模式 | 场景 |
|--------|------|------|
| 0a | `data-dt-package_name` + `.{0,500}?` + `data-dt-version_code` | APKPure 详情页 (包名关联+500字符限定) |
| 0b | `data-version_code="NNNNNN"` | 带下划线变体 |
| 1 | `variant code: NNNNNN` | APKCombo |
| 2 | `x.y.z (NNNNNN)` | 括号内嵌 |
| 3a | `data-dt-versioncode="NNNNNN"` | APKPure 搜索页 |
| 3b | `data-versioncode="NNNNNN"` | |
| 4 | `<meta>` versionCode | HTML meta |
| 5 | `"versionCode": "NNNNNN"` | JSON 内嵌 |
| 6 | `Version Code: NNNNNN` | 纯文本标签 |
| 7 | `data-app-versioncode` | 备选 data 属性 |
| 8 | `_NNNNNN_.apk` / `-NNNNNN.apk` | APK 文件名 |
| 9 | `<dt>Version Code</dt><dd>NNNNNN</dd>` | 定义列表 |
| 10 | `version/ver/vc/code : NNNNN` | 兜底 |

### 版本名解析 (5 策略)

1. class 名含 "version" 的元素 → 精确匹配 x.y.z
2. data-* 属性 (data-dt-version, data-version 等)
3. Schema.org itemprop="version"
4. 全文 "Version: x.y.z" / "版本：x.y.z"
5. 兜底 `>x.y.z<` 标签内嵌

---

## 七、TTL 缓存系统

```
ScraperCache
├── 内存存储: { "pkg::mode" → (FetchResult, expiry_time) }
├── TTL 可配置: cache_ttl_seconds (默认 300s = 5分钟)
├── 粒度: 按 package + mode (fast/slow/all) 分别缓存
├── 管理端点: POST /api/cache/clear
│
└── 命中流程:
      query_fast("com.tencent.ig") → cache.get("com.tencent.ig", "fast")
      → 命中 → 直接返回 FetchResult (跳过全部 HTTP 请求)
      → 未命中 → 执行爬虫 → cache.set()
```

---

## 八、多包名并发排查

```
前端:
  多行/逗号/分号分隔 → packages.length > 1
  → POST /api/fetch/batch  {packages: [...], mode: "fast"}

后端 query_batch():
  Semaphore(batch_concurrency=5) 限流
  asyncio.gather 全部协程同时启动, Semaphore 控制并发度
  每个包名内部走对应 query_fn (继承 ScraperCache)
```

---

## 九、批量 Excel 处理

### 完整流程

```
[用户] 上传 Excel (.xlsx/.xls)
  │ 必须: package_name 列
  │ 可选: expected_version_code, expected_version_name
  ▼
[后端] 解析 Excel → 创建 BatchTask → asyncio.create_task 启动
  │ 自动检测列: package_name/package/packagename
  ← {"task_id": "batch_xxx", "total": N, "filename": "xxx.xlsx"}
  │
  ▼
[前端] WebSocket ws://host/ws/{task_id} 订阅进度
  │
  ▼
[后端] Semaphore(5) + as_completed 并发
  │ 每包名: query_fast(pkg) → best_version_code + compare_status
  │ 进度推送: 每 5 个完成推送一次
  │ 流式持久化: 结果写入临时 JSONL 文件
  │
  ▼ 100%
[后端] 在原 Excel 写入结果 (保留原始样式):
  │ 三列: 排查时间 | 当前后台版本号(vc) | 对比状态
  │ 样式: 蓝色表头 + 白色粗体 + 居中
  │
  ▼
[前端] 弹窗显示汇总 → 下载结果 Excel
  │
  ▼
[后端] BatchManager._cleanup_temp(task) — 清理临时 JSONL
```

### 控制

```
暂停: POST /api/batch/{id}/pause  → BatchTask.status = "paused"
继续: POST /api/batch/{id}/resume → BatchTask.status = "running"
取消: POST /api/batch/{id}/cancel → BatchManager._cleanup_temp()
```

---

## 十、下载系统

### 下载链接提取

| 源 | 方法 | 链接格式 |
|----|------|---------|
| APKPure | URL 构造 | `apkpure.net/cn/{slug}/{pkg}/download` |
| APKCombo | URL 构造 (中文) | `apkcombo.com/zh/{slug}/{pkg}/download/phone-{ver}-apk` |
| APKCombo | Fallback (无slug) | `apkcombo.com/zh/{pkg}/download` |
| APKMirror | StealthySession 多步跳转 | 动态 |
| APKVision | StealthySession 页面提取 | 动态 |
| Google Play | ❌ | 不提供 |

### 下载后端

| 源 | aiohttp 直下 | Playwright 浏览器下载 | 原因 |
|----|-------------|---------------------|------|
| APKCombo | ❌ | ✅ 浏览器下载页 | 防盗链 |
| APKPure | ❌ | ✅ 浏览器下载页 | d.apkpure.net 强防盗链 |
| APKMirror | ⚠️ | ✅ | CF 保护 |
| APKVision | ⚠️ | ✅ | 同上 |

### aiohttp 断点续传

```
DownloadManager._download():
  检查 .part 文件 → Range: bytes={downloaded_size}-
  → aiohttp 流式写入 (chunk_size 可配, 默认 1MB)
  → 每 0.5s 计算速度/进度 → WebSocket 推送
  → 完成: 重命名 .part → .apk
```

---

## 十一、记忆化

```
输入包名 → 点击 "应用上次版本"
  GET /api/memo/{pkg} → SQLite user_history 查询
  → 自动填充 version_code + version_name

排查完成 (save_memo=true) → POST /api/memo → SQLite UPSERT

规则:
  ✅ 单包手动排查 | ❌ 多包名 | ❌ Excel批量
```

---

## 十二、完整 API

### 爬取
| 端点 | 方法 | 模式 |
|------|------|------|
| `/api/fetch` | POST | 快速 (默认) |
| `/api/fetch/fast` | POST | 快速 |
| `/api/fetch/slow` | POST | 慢速 (同步阻塞) |
| `/api/fetch/slow/async` | POST | 慢速异步 |
| `/api/fetch/slow/result/{id}` | GET | 查询异步结果 |
| `/api/fetch/all` | POST | 全量 |
| `/api/fetch/batch` | POST | 多包名并发 |

### 下载
| 端点 | 方法 |
|------|------|
| `/api/extract-links` | POST |
| `/api/download` | POST |
| `/api/download/batch` | POST |
| `/api/download/tasks` | GET |
| `/api/download/{id}/pause` | POST |
| `/api/download/{id}/cancel` | POST |

### 批量 Excel
| 端点 | 方法 |
|------|------|
| `/api/batch/upload` | POST |
| `/api/batch/{id}` | GET |
| `/api/batch/{id}/pause` | POST |
| `/api/batch/{id}/resume` | POST |
| `/api/batch/{id}/cancel` | POST |
| `/api/batch/{id}/download` | GET |

### 记忆化 & 配置 & 缓存
| 端点 | 方法 |
|------|------|
| `/api/memo/{pkg}` | GET |
| `/api/memo` | POST / GET |
| `/api/config` | GET / PATCH |
| `/api/cache/clear` | POST |

### WebSocket
| 端点 | 用途 |
|------|------|
| `/api/ws` | 全局：下载进度 + 批量通知 |
| `/api/ws/{task_id}` | 任务专用：批量进度 + 慢速异步完成 |

---

## 十三、配置文件

```json
{
  "download_path": "./downloads",
  "download_concurrency": 3,
  "download_chunk_size": 1048576,
  "scraper_concurrency": 4,
  "playwright_concurrency": 2,
  "batch_concurrency": 5,
  "cache_ttl_seconds": 300,
  "request_interval": 1,
  "retry_times": 2,
  "retry_delay": 1,
  "proxy": "http://127.0.0.1:7897",
  "enabled_sites": ["google_play", "apkpure", "apkcombo", "apkmirror", "apkvision"],
  "google_play_cookie_path": "",
  "log_level": "INFO",
  "log_retention_days": 30,
  "auto_cleanup_days": 7,
  "language": "zh",
  "request_timeout": 10,
  "stealth_timeout": 60
}
```

### 完整配置项说明

| 配置项 | 默认值 | 分类 | 说明 |
|--------|--------|------|------|
| `download_path` | "./downloads" | 下载 | APK 保存目录 |
| `download_concurrency` | 3 | 下载 | 同时下载任务数 |
| `download_chunk_size` | 1048576 | 下载 | 下载缓冲区 (1MB) |
| `scraper_concurrency` | 4 | 爬虫 | 同时爬取站点数 |
| `playwright_concurrency` | 2 | 爬虫 | 同时打开的 Playwright 页面数 |
| `batch_concurrency` | 5 | 批量 | 批量任务内部并发包名数 |
| `cache_ttl_seconds` | 300 | 缓存 | 爬虫结果缓存有效期 (秒) |
| `request_interval` | 1 | 爬虫 | 请求间隔 (秒) |
| `retry_times` | 2 | 爬虫 | 失败重试次数 |
| `retry_delay` | 1 | 爬虫 | 重试延迟 (秒) |
| `proxy` | "http://127.0.0.1:7897" | 网络 | 代理地址 |
| `enabled_sites` | [5站点] | 爬虫 | 启用的数据源 |
| `google_play_cookie_path` | "" | 爬虫 | GP Cookie 文件路径 |
| `log_level` | "INFO" | 日志 | 日志级别 |
| `log_retention_days` | 30 | 日志 | 日志保留天数 |
| `auto_cleanup_days` | 7 | 清理 | 自动清理天数 |
| `language` | "zh" | 界面 | 界面语言 |
| `request_timeout` | 10 | 网络 | Fetcher 请求超时 (秒) |
| `stealth_timeout` | 60 | 网络 | StealthySession 超时 (秒) |

---

## 十四、数据库

### user_history
| 字段 | 类型 | 说明 |
|------|------|------|
| package_name | TEXT PK | 包名 |
| version_code | TEXT | 版本号 |
| version_name | TEXT | 版本名 |
| updated_at | TIMESTAMP | 更新时间 |

### download_tasks
| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT PK | 任务 ID |
| url | TEXT | 下载 URL |
| package_name | TEXT | 包名 |
| version | TEXT | 版本 |
| arch | TEXT | 架构 |
| save_path | TEXT | 保存路径 |
| total_size | INTEGER | 总大小 |
| downloaded_size | INTEGER | 已下载 |
| status | TEXT | pending/downloading/paused/completed/error |
| error | TEXT | 错误信息 |
| created_at | TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | 更新时间 |

### batch_tasks
| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT PK | 任务 ID |
| filename | TEXT | 原始文件名 |
| total_rows | INTEGER | 总行数 |
| completed_rows | INTEGER | 已完成行数 |
| status | TEXT | pending/running/paused/completed/cancelled |
| result_path | TEXT | 结果文件路径 |
| error | TEXT | 错误信息 |
| created_at | TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | 更新时间 |

### SQLite 优化
- `PRAGMA journal_mode=WAL`
- `PRAGMA synchronous=NORMAL`
- `PRAGMA foreign_keys=ON`

---

## 十五、项目结构

```
game-package-crawler/
├── backend/
│   ├── main.py              FastAPI 入口 + 生命周期 (v2.6.0)
│   ├── config.py            pydantic Settings (18 配置项)
│   ├── logging_setup.py     loguru 日志 (按天轮转, 30天保留, gz压缩)
│   ├── api/
│   │   ├── routes.py        28 个 REST/WS 端点
│   │   └── websocket.py     WSManager (按task_id分组 + 全局广播)
│   ├── models/
│   │   └── schemas.py       ApkInfo, FetchResult, DownloadTask, CompareStatus
│   │                          + app_name + whats_new (v2.6)
│   ├── scrapers/
│   │   ├── base.py          BaseScraper (ABC)
│   │   ├── google_play.py   lang=zh + app_name + whats_new 提取 (v2.6)
│   │   ├── apkpure.py       搜索→详情 + _extract_app_name + _extract_whats_new (v2.6)
│   │   ├── apkcombo.py      API 302 + _extract_app_name + _extract_whats_new (v2.6)
│   │   ├── apkmirror.py     搜索→详情多步 (Fetcher → Stealthy 降级)
│   │   ├── apkvision.py     StealthySession + CF bypass
│   │   └── selectors.yaml   选择器配置 (热加载)
│   ├── core/
│   │   ├── http_client.py   三级后端: Fetcher / StealthySession / JS Render
│   │   ├── parser.py        版本号 13 模式 + 版本名 5 策略
│   │   ├── version.py       标准化 + compare + best 判定
│   │   ├── orchestrator.py  快/慢/全量调度 + TTL 缓存 + app_name/whats_new 聚合 (v2.6)
│   │   ├── browser_manager.py Playwright 持久上下文
│   │   └── cache.py         ScraperCache (TTL) + SlowTaskStore (异步)
│   ├── download/
│   │   ├── extractors.py    链接提取 + 中文版 /zh/ + slug fallback (v2.6)
│   │   └── manager.py       aiohttp + Playwright 降级 + 断点续传
│   ├── batch/
│   │   └── manager.py       BatchManager (Semaphore 并发 + JSONL 流式 + Excel 写回)
│   ├── memo/
│   │   └── store.py         SQLite CRUD
│   └── db/
│       └── database.py      SQLite 连接 + 3 张表
├── frontend/
│   └── src/
│       ├── styles/global.css  全局主题系统 + Element Plus 覆盖 (v2.6, 350行)
│       ├── App.vue            毛玻璃头部 + 动态背景 + 主题切换 (v2.6)
│       ├── main.ts            Vue3 + Pinia + Element Plus + global.css
│       ├── components/
│       │   ├── PackageInput.vue   发光输入 + emoji + 幽默 Toast (v2.6)
│       │   ├── ResultTable.vue    表格 + 应用信息卡片 app_name/whats_new (v2.6)
│       │   ├── BatchPanel.vue     渐变进度条 + emoji 汇总弹窗 (v2.6)
│       │   ├── DownloadQueue.vue  卡片化布局 + 动画 (v2.6)
│       │   └── SettingsPanel.vue  卡片分组 + 主题开关 + 代理测试 (v2.6)
│       ├── stores/app.ts         Pinia (含 darkMode 主题状态)
│       └── composables/useWebSocket.ts
├── tests/
│   ├── test_parser.py
│   ├── test_version.py
│   └── test_memo.py
├── config.json
├── build.spec               PyInstaller 单文件
├── launcher.py
├── requirements.txt
├── 系统工作流程2.6.md       本文档
├── 版本总历史.md             全版本历史
├── 动态JS策略.md
└── 实施计划.md
```

---

## 十六、启动方式

```bash
# 源码启动
python -m uvicorn backend.main:app --host 127.0.0.1 --port 8000

# EXE 打包
cd frontend && npm install && npm run build && cd ..
pyinstaller --clean --noconfirm build.spec

# 访问
http://127.0.0.1:8000        Web 前端
http://127.0.0.1:8000/docs   Swagger API 文档

# 测试
pytest tests/ -v             44+ passed
```

---

## 十七、版本历史

### v2.6 (2026-06-04) — 前端美化 + 数据增强
- 🎨 **前端全面美化**: CSS 变量主题系统 (深色/浅色)、毛玻璃效果、6 种关键帧动画、Element Plus 全局主题覆盖
- 📝 **游戏中文名 & 更新内容**: Google Play 中文站 + APKPure/APKCombo HTML 解析 → 前端应用信息卡片
- 🔧 **APKCombo URL 修复**: 路径黑名单过滤 API URL + slug 缺失 fallback
- 🐛 **Bug 修复**: `BatchTask` → `BatchManager` 静态方法引用修正 (4 处)
- 📄 **文档**: 新增 `系统工作流程2.5.md`、`系统工作流程2.6.md`、`版本总历史.md`

### v2.5 (2026-06-04)
- Bug 修复: BatchTask → BatchManager 错误引用
- APKCombo 中文版下载页 URL `/zh/`
- 版本号统一 v2.5

### v2.4 (2026-06-03)
- TTL 爬虫缓存 + 慢速异步任务 + 批量内部并发
- 批量结果流式持久化 + Excel 导出增强
- 下载缓冲区可配 + SQLite 性能优化
- HTTP 客户端三级后端重构

### v2.3
- 批量三列写回 + 完成弹窗汇总
- 版本号解析修复 (包名关联 + 500字符限定)

### v2.2
- 日志格式修复 + Playwright 后台启动

### v2.1
- 多包名并发排查 + 版本名/版本号双重对比

### v2.0
- FastAPI + Vue3 架构 + 5 站点爬虫 + aiohttp 断点续传 + PyInstaller 打包
